#include <stdio.h>

int main()
{


/* Zad 12
  double a, x;
    int n;

    printf("Podaj nieujemną liczbę: ");
    scanf("%lf", &a);
    printf("Podaj liczbę powtórzeń: ");
    scanf("%d", &n);

    if (a < 0) {
        printf("Liczba musi być nieujemna!\n");
        return 1;
    }

    x = a / 2; // pierwsze przybliżenie

    for (int i = 0; i < n; i++) {
        x = 0.5 * (x + a / x);
    }

    printf("Pierwiastek z %.4lf ≈ %.8lf\n", a, x);
    return 0;
*/

/* Zad 11
 int N;
    printf("Podaj liczbę wyrazów szeregu: ");
    scanf("%d", &N);

    double suma = 0;
    for (int n = 0; n < N; n++) {
        suma += pow(-1, n) / (2 * n + 1);
    }

    printf("π ≈ %.10lf\n", 4 * suma);
    return 0;
*/

/* Zad 10

   double x = M_PI / 3; 
    int N;
    printf("Podaj liczbę wyrazów szeregu: ");
    scanf("%d", &N);

    double wynik = 0, silnia;
    for (int n = 0; n < N; n++) {
        silnia = 1;
        for (int k = 1; k <= 2 * n + 1; k++) silnia *= k;
        wynik += pow(-1, n) * pow(x, 2 * n + 1) / silnia;
    }

    printf("sin(π/3) ≈ %.8lf\n", wynik);
    return 0;
    */

/* Zad 9.2

    int n, i = 1;
    long silnia = 1;

    printf("Podaj liczbę (0–10): ");
    scanf("%d", &n);

    if (n < 0 || n > 10) {
        printf("Liczba poza zakresem!\n");
        return 1;
    }

    if (n == 0) {
        printf("0! = 1\n");
        return 0;
    }

    do {
        silnia *= i;
        i++;
    } while (i <= n);

    printf("%d! = %l\n", n, silnia);
    return 0;
    */

*/
/* Zad 9.1

    int n, i = 1;
    long silnia = 1;

    printf("Podaj liczbę (0–10): ");
    scanf("%d", &n);

    if (n < 0 || n > 10) {
        printf("Liczba poza zakresem!\n");
        return 1;
    }

    while (i <= n) {
        silnia *= i;
        i++;
    }

    printf("%d! = %l\n", n, silnia);
    return 0;
    */

/* Zad 8
 int a1, an, r, s = 0;
    scanf("%d%d%d", &a1, &an, &r);
    for (int i = a1; i <= an; i += r) s += i;
    printf("%d", s);
    */

/* Zad 7.2
  double a1, an, q, suma = 0, i;

    printf("Podaj pierwszy wyraz ciągu: ");
    scanf("%lf", &a1);
    printf("Podaj ostatni wyraz ciągu: ");
    scanf("%lf", &an);
    printf("Podaj iloraz: ");
    scanf("%lf", &q);

    if (q == 0 || a1 == 0) {
        printf("Nieprawidłowe dane!\n");
        return 1;
    }

    i = a1;
    do {
        suma += i;
        i *= q;
    } while ((q > 1) ? (i <= an) : (i >= an));

    printf("Suma ciągu geometrycznego (do...while): %.2lf\n", suma);
    return 0;
    */

/* Zad 7.1
   double a1, an, q, suma = 0, i;

    printf("Podaj pierwszy wyraz ciągu: ");
    scanf("%lf", &a1);
    printf("Podaj ostatni wyraz ciągu: ");
    scanf("%lf", &an);
    printf("Podaj iloraz: ");
    scanf("%lf", &q);

    if (q == 0 || a1 == 0) {
        printf("Nieprawidłowe dane!\n");
        return 1;
    }

    i = a1;
    while ((q > 1) ? (i <= an) : (i >= an)) {
        suma += i;
        i *= q;
    }

    printf("Suma ciągu geometrycznego (while): %.2lf\n", suma);
    return 0;
    */

/* Zad 6
 double a1, an, q, suma = 0;
    
    printf("Podaj pierwszy wyraz ciągu (a1): ");
    scanf("%lf", &a1);
    printf("Podaj ostatni wyraz ciągu (an): ");
    scanf("%lf", &an);
    printf("Podaj iloraz (q): ");
    scanf("%lf", &q);

    if (q == 0 || a1 == 0) {
        printf("Nieprawidłowe dane! q i a1 nie mogą być równe 0.\n");
        return 1;
    }

  
    for (double i = a1; (q > 1) ? (i <= an) : (i >= an); i *= q) {
        suma += i;
    }

    printf("Suma ciągu geometrycznego: %.2lf\n", suma);
    return 0;
    */

/* Zad 5.2
  int a1, an, r;
    int suma = 0, i;

    printf("Podaj pierwszy wyraz ciągu: ");
    scanf("%d", &a1);
    printf("Podaj ostatni wyraz ciągu: ");
    scanf("%d", &an);
    printf("Podaj różnicę ciągu: ");
    scanf("%d", &r);

    i = a1;
    do {
        suma += i;
        i += r;
    } while (i <= an);

    printf("Suma ciągu (do-while): %d\n", suma);
    return 0;
    */

 /* Zad 5.1
  int a1, an, r;
    int suma = 0, i;

    printf("Podaj pierwszy wyraz ciągu: ");
    scanf("%d", &a1);
    printf("Podaj ostatni wyraz ciągu: ");
    scanf("%d", &an);
    printf("Podaj różnicę ciągu: ");
    scanf("%d", &r);

    i = a1;
    while (i <= an) {
        suma += i;
        i += r;
    }

    printf("Suma ciągu (while): %d\n", suma);
    return 0;
    */


  /* Zad 4
  int a1, an, r;
    int suma = 0;

    printf("Podaj pierwszy wyraz ciągu: ");
    scanf("%d", &a1);
    printf("Podaj ostatni wyraz ciągu: ");
    scanf("%d", &an);
    printf("Podaj różnicę ciągu: ");
    scanf("%d", &r);

    for (int i = a1; i <= an; i += r) {
        suma += i;
    }

    printf("Suma ciągu: %d\n", suma);
    return 0;

   */



    /* Zad 2
   int i;

    for (i = 1; i <= 7; i++) {
        printf("%d ", i);
    }
    printf("\n");

    i = 1;
    while (i <= 7) {
        printf("%d ", i);
        i++;
    }
    printf("\n");

    i = 1;
    do {
        printf("%d ", i);
        i++;
    } while (i <= 7);
    printf("\n");

    return 0;
    */
    
}
